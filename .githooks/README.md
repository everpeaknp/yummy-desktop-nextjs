# Git hooks

Enable once per clone (optional, prevents `cursoragent` on commits):

```bash
git config core.hooksPath .githooks
```

On Windows with Git Bash, hooks run automatically when `core.hooksPath` is set.
